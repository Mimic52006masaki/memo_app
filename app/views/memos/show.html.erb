<div class="container mx-auto max-w-3xl bg-white rounded-xl shadow-md p-6 mt-8">
  <h1 class="text-3xl font-bold mb-4"><%= @memo.title %></h1>
  <p class="whitespace-pre-wrap text-gray-800 mb-6"><%= @memo.content %></p>

  <div class="flex space-x-4">
    <%= link_to "編集", edit_memo_path(@memo), class: "px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700" %>
    <%= link_to "一覧に戻る", memos_path, class: "px-4 py-2 border border-gray-400 rounded hover:bg-gray-100" %>
  </div>
</div>
